#ifndef LINEAR_SEARCH_LIST_H
#define LINEAR_SEARCH_LIST_H

#include <stdbool.h>
#include <stddef.h>

bool linear_search(const int *haystack, size_t length, int needle);

#endif // LINEAR_SEARCH_LIST_H
